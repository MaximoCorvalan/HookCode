/** Define un enlace de navegación dentro de la página. */
export interface NavigationItem {
  /** Identificador de la sección de destino, sin el prefijo #. */
  id: string
  /** Texto visible del enlace. */
  label: string
}
