/** Nodo de la red: posición, velocidad y radio en píxeles CSS. */
export interface Particle { x: number; y: number; vx: number; vy: number; radius: number }
/** Posición del cursor relativa a la portada. */
export interface ParticlePointer { x: number; y: number; active: boolean }
